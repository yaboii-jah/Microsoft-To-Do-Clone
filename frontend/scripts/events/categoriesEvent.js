export function categoriesEventListener (generateTask) { 
  const events = ['MyDay', 'Important', 'Planned', 'Assigned', 'Tasks']
  

  events.forEach((event, index) => {
    document.querySelectorAll('.category')[index].addEventListener('click', () => {
       
    })
  })
}